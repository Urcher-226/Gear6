function performlogin() {
    const boy = document.getElementById("searchInput").value;
    const bad = document.getElementById("search").value;
    var a="kali";
    var b="kali";
    if ((boy==a) && (bad==b)) {
        open('index1.html');
    }
    else{
        console.log('Stop looking in private site...');
    }
}